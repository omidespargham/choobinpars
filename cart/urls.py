from django.urls import path
from . import views

app_name = 'cart'
urlpatterns = [
    # path('', views.CartShowView.as_view(),name='cart_show'),
    # path('add/<int:product_id>/', views.CartAddView.as_view(),name='cart_add'),
]